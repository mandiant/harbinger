name: Golang build

on:
  push:

jobs:
  test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3

    - uses: actions/setup-go@v5

    - name: Go build
      run: go build *.go
      working-directory: ./harbinger/src/harbinger/connectors/mythic_go/
